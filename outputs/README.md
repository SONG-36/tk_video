# outputs 目录职责

本目录只负责存放 Codex 生成的最终内容资产。当前阶段不生成任何真实内容，仅保留目录占位。

本目录不负责保存规则、输入数据、任务指令或未规划的视频目录，也不是业务定义来源。

未来输出必须使用 `outputs/{product_series_id}/{batch_id}/`，并只创建批次规划中实际存在的视频目录。目录结构和内容始终服从根目录 `00_design.md`。
