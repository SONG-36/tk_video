# 规则阅读版

> 本文档来源于 `00_design.md`，仅用于阅读，不是权威源。

这里按规则主题拆分第 10—16 章。机器可读骨架位于根目录 `rules/`；任何冲突均以 `00_design.md` 为准。

本目录不负责定义新规则，也不替代根目录 `rules/` 中的机器可读 JSON 或唯一权威源。

未来扩展必须先更新 `00_design.md`，再同步对应规则阅读版。
