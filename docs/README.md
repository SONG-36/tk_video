# docs 目录职责

> 本文档来源于 `00_design.md`，仅用于阅读，不是权威源。

本目录负责把权威源按流程、页面、状态、数据、规则、输出和案例拆分为便于阅读的文档，并记录发现但未擅自修正的设计问题。

本目录不负责定义新规则、覆盖 `00_design.md` 或承载实现代码。冲突时以根目录 `00_design.md` 为准。

未来扩展时，必须先修改 `00_design.md`，再同步对应阅读版，并通过设计同步检查。
