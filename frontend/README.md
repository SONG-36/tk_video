# frontend 目录职责

本目录负责严格对应 `00_design.md` 第 7—9 章的页面、流程闸门、表单、报告展示、资产卡片、状态展示和前端类型。

本目录不负责自由聊天、业务规则定义、后端判断、任务内容生成或直接控制 Codex。规则只能读取后端结果和 `rules/` 的稳定接口，不得散落在页面中。

未来可在既有页面目录中增加 UI、路由、状态管理和测试，但不得改变页面顺序、状态含义或型号绑定约束。
