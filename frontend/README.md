# frontend 前端职责

前端是 `00_design.md` 定义的流程控制器和结果展示层，不是自由聊天框。它负责收集结构化输入、展示判断结果、执行人工确认、显示阻断原因和读取输出。

前端不得定义业务规则、绕过状态闸门、直接控制 Codex 或调用外部模型。未来实现只能读取后端结果并按 Schema 组织数据；当前目录仅有职责 README，没有 UI、路由、状态管理、组件或 API 客户端实现。
