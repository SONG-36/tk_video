# 页面目录与顺序

页面严格按 `00_design.md` 第 4、7—9 章排列：商品列表 → 新建商品 → 类目确认 → 型号 / SKU 录入 → 型号差异判断 → 本次拍摄型号选择 → 型号级商品画像 → 型号级判断报告 → 材料检查 → 型号级图片资产 → 创意方向 → 视频批次 → 镜头资产 → 任务包导出 → 输出结果。

每个页面 README 只描述目标、字段、动作、输出、闸门、ID 绑定和依赖，不代表 UI 已实现。`target_variant_selected` 之后的页面都必须持续显示并绑定当前目标型号。
