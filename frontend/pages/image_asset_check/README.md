# 型号级产品图片资产页

- 页面目标：上传并标注目标型号图片，评估完整度与生成能力。
- 输入字段：图片文件、类型、视角、用途、质量、必需性、备注和型号归属。
- 输出数据：图片资产清单、完整度等级、缺图清单、失真风险和能力限制。
- 通过条件：图片已上传或明确缺失；每图完成类型和关键用途；系统生成等级、缺图及限制。
- 状态依赖：材料通过后进入，通过后为 `variant_image_assets_checked`；少于三张禁止镜头级提示词。
