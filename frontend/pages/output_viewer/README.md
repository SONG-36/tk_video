# 输出结果查看页

- 页面目标：读取并展示 Codex 生成的批次内容资产。
- 输入字段：`product_series_id`、`batch_id`、批次视频清单和 `outputs/` 下的实际文件。
- 输出数据：文件展示状态、通过/重做标记和重新导出意图。
- 通过条件：路径绑定正确，展示字段符合质量规则；缺失文件明确提示。
- 状态依赖：任务包导出后进入；处理期间为 `codex_processing`，结果存在为 `codex_output_ready`。无批次规划不得读取 `video_04/05`。
