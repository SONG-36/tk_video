# 型号 / SKU 录入页

- 页面目标：记录系列下所有可单独选择、拍摄和销售的型号。
- 输入字段：型号名、卖家 SKU、平台 SKU、颜色、尺寸、核心参数、配件、价格、型号图片；可选 Revision 默认不启用。
- 输出数据：`variants[]`，每项有唯一 `variant_id`。
- 通过条件：至少存在一个完整型号；单型号也创建默认型号。
- 状态依赖：类目确认后进入，通过后为 `variants_recorded`。
