# 型号级商品画像补全页

- 页面目标：结构化补齐影响分类、分型和生产的目标型号特征。
- 输入字段：体积、真人、大场景、功能、审美依赖、对比、包装；电子、电池、人体、儿童、宠物、食品、高价、合规；至少一个卖点类型。
- 输出数据：绑定 `product_series_id` 与 `variant_id` 的 `product_profile`。
- 通过条件：基础和风险字段完整、至少一个卖点、无严重冲突；轻微冲突需确认。
- 状态依赖：目标型号选择后进入，通过后为 `variant_profile_completed`。
