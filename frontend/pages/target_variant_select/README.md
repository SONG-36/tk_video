# 本次拍摄型号选择页

- 页面目标：为本次内容生产选择唯一目标型号。
- 输入字段：商品系列、可选型号名、`variant_id`、卖家/平台 SKU 和关键差异摘要。
- 输出数据：`selected_variant_id`。
- 通过条件：所选 ID 存在于当前系列 `variants[]` 且用户确认。
- 状态依赖：差异检查后进入，通过后为 `target_variant_selected`；切换选择会使后续型号级结果失效并复验。
