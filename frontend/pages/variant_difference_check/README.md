# 型号差异判断页

- 页面目标：判断字段可共享性及是否必须分型号制作内容。
- 输入字段：型号/SKU、外观颜色、尺寸重量、功能参数、按钮接口、包装配件、价格卖点、图片、分型、合规安全信息。
- 输出数据：`variant_difference_report`，含是否检查、是否隔离及不可共享字段。
- 通过条件：所有已录入型号完成比较并确认隔离结论。
- 状态依赖：`variants_recorded` 后进入，通过后为 `variant_differences_checked`。
