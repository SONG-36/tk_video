import{renderToStaticMarkup}from'react-dom/server';import{describe,it,expect}from'vitest';import{MemoryRouter,Routes,Route}from'react-router';import Page from'./Page';import{getIssues}from'./fixture';describe('category_confirm',()=>{it('renders candidates and context',()=>{const m=renderToStaticMarkup(<MemoryRouter initialEntries={['/products/p_001/category']}><Routes><Route path="/products/:productSeriesId/category" element={<Page/>}/></Routes></MemoryRouter>);expect(m).toContain('类目确认页');expect(m).toContain('Automotive &amp; Motorcycle');expect(m).toContain('手动修改类目');expect(m).toContain('<details class="local-debug">')});it('blocks absent selection and confirmation',()=>expect(getIssues('',false)).toHaveLength(2));it('accepts local confirmed category',()=>expect(getIssues('Automotive',true)).toEqual([]))})
