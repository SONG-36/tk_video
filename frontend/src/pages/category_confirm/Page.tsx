import { RoutePlaceholder } from '../RoutePlaceholder'
import { pageByKey } from '../pageMeta'

export default function CategoryConfirmPage() {
  return <RoutePlaceholder page={pageByKey.category_confirm} />
}
