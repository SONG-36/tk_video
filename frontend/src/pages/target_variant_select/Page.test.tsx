import{renderToStaticMarkup}from'react-dom/server';import{describe,it,expect}from'vitest';import{MemoryRouter}from'react-router';import Page from'./Page';import{getIssues,invalidatedObjects}from'./fixture';describe('target_variant_select',()=>{it('renders variants and invalidation warning',()=>{const m=renderToStaticMarkup(<MemoryRouter><Page/></MemoryRouter>);expect(m).toContain('本次拍摄型号选择页');expect(m).toContain('sku_001');expect(m).toContain('切换型号后');expect(m).toContain('<details class="local-debug">')});it('blocks missing selection',()=>expect(getIssues('')).toHaveLength(1));it('accepts existing selection',()=>expect(getIssues('sku_001')).toEqual([]));it('lists invalidated objects',()=>expect(invalidatedObjects).toContain('output_viewer'))})
