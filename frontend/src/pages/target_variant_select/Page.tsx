import { RoutePlaceholder } from '../RoutePlaceholder'
import { pageByKey } from '../pageMeta'

export default function TargetVariantSelectPage() {
  return <RoutePlaceholder page={pageByKey.target_variant_select} />
}
