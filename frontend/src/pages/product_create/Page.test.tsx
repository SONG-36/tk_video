import {renderToStaticMarkup} from'react-dom/server';import{describe,it,expect}from'vitest';import{MemoryRouter}from'react-router';import Page from'./Page';import{getIssues,initialDraft}from'./fixture';describe('product_create',()=>{it('renders all core fields',()=>{const m=renderToStaticMarkup(<MemoryRouter><Page/></MemoryRouter>);expect(m).toContain('新建商品页');expect(m).toContain('产品名称');expect(m).toContain('尝试创建');expect(m).toContain('<details class="local-debug">')});it('blocks missing required values',()=>expect(getIssues(initialDraft)).toContain('产品名称为空。'));it('allows a complete local draft',()=>expect(getIssues({...initialDraft,product_name:'车载吸尘器'})).toEqual([]))})
