import { RoutePlaceholder } from '../RoutePlaceholder'
import { pageByKey } from '../pageMeta'

export default function ProductCreatePage() {
  return <RoutePlaceholder page={pageByKey.product_create} />
}
