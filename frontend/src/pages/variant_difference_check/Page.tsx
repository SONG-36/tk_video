import { RoutePlaceholder } from '../RoutePlaceholder'
import { pageByKey } from '../pageMeta'

export default function VariantDifferenceCheckPage() {
  return <RoutePlaceholder page={pageByKey.variant_difference_check} />
}
