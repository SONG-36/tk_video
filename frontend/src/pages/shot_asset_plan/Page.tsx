import { RoutePlaceholder } from '../RoutePlaceholder'
import { pageByKey } from '../pageMeta'

export default function ShotAssetPlanPage() {
  return <RoutePlaceholder page={pageByKey.shot_asset_plan} />
}
