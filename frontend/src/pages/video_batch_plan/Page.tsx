import { RoutePlaceholder } from '../RoutePlaceholder'
import { pageByKey } from '../pageMeta'

export default function VideoBatchPlanPage() {
  return <RoutePlaceholder page={pageByKey.video_batch_plan} />
}
