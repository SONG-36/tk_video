import{renderToStaticMarkup}from'react-dom/server';import{describe,it,expect}from'vitest';import{MemoryRouter}from'react-router';import Page from'./Page';import{addVariant,getIssues,initialVariants}from'./fixture';describe('variant_sku_input',()=>{it('renders variant fields',()=>{const m=renderToStaticMarkup(<MemoryRouter><Page/></MemoryRouter>);expect(m).toContain('型号 / SKU 录入页');expect(m).toContain('VAC-BLACK-01');expect(m).toContain('核心参数');expect(m).toContain('<details class="local-debug">')});it('adds local variant',()=>expect(addVariant(initialVariants)).toHaveLength(2));it('blocks empty list',()=>expect(getIssues([])).toContain('至少需要一个型号。'))})
