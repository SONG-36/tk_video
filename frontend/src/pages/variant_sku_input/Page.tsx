import { RoutePlaceholder } from '../RoutePlaceholder'
import { pageByKey } from '../pageMeta'

export default function VariantSkuInputPage() {
  return <RoutePlaceholder page={pageByKey.variant_sku_input} />
}
