import { RoutePlaceholder } from '../RoutePlaceholder'
import { pageByKey } from '../pageMeta'

export default function JudgementReportPage() {
  return <RoutePlaceholder page={pageByKey.judgement_report} />
}
