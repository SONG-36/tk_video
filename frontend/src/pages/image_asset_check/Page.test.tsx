import{renderToStaticMarkup}from'react-dom/server';import{describe,it,expect}from'vitest';import{MemoryRouter}from'react-router';import Page from'./Page';import{getIssues,images}from'./fixture';describe('image_asset_check',()=>{it('renders image assets and capability',()=>{const m=renderToStaticMarkup(<MemoryRouter><Page/></MemoryRouter>);expect(m).toContain('型号级产品图片资产页');expect(m).toContain('img_001');expect(m).toContain('完整度');expect(m).toContain('<details class="local-debug">')});it('blocks too few images',()=>expect(getIssues(images.slice(0,2),'sku')).toContain('图片少于 3 张，禁止高质量镜头提示词。'));it('accepts fixture',()=>expect(getIssues(images,'sku_001')).toEqual([]))})
