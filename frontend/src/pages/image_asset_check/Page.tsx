import { RoutePlaceholder } from '../RoutePlaceholder'
import { pageByKey } from '../pageMeta'

export default function ImageAssetCheckPage() {
  return <RoutePlaceholder page={pageByKey.image_asset_check} />
}
