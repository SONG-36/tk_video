import { RoutePlaceholder } from '../RoutePlaceholder'
import { pageByKey } from '../pageMeta'

export default function VariantProfilePage() {
  return <RoutePlaceholder page={pageByKey.variant_profile} />
}
