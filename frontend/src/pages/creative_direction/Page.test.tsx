import{renderToStaticMarkup}from'react-dom/server';import{describe,it,expect}from'vitest';import{MemoryRouter}from'react-router';import Page from'./Page';import{direction,getIssues}from'./fixture';describe('creative_direction',()=>{it('renders chapter 17/18 fields',()=>{const m=renderToStaticMarkup(<MemoryRouter><Page/></MemoryRouter>);expect(m).toContain('创意方向设定页');expect(m).toContain('TikTok 原生结构');expect(m).toContain('爆款模式迁移');expect(m).toContain('自由创意输入');expect(m).toContain('<details class="local-debug">')});it('accepts fixture',()=>expect(getIssues(direction)).toEqual([]));it('blocks missing style',()=>expect(getIssues({...direction,creative_direction:{...direction.creative_direction,selected_styles:[]}})).toContain('未选择创意风格。'))})
