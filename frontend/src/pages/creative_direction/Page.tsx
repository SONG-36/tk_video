import { RoutePlaceholder } from '../RoutePlaceholder'
import { pageByKey } from '../pageMeta'

export default function CreativeDirectionPage() {
  return <RoutePlaceholder page={pageByKey.creative_direction} />
}
