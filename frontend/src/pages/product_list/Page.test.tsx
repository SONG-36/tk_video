import { renderToStaticMarkup } from 'react-dom/server'; import { describe, expect, it } from 'vitest'; import { MemoryRouter } from 'react-router'; import Page from './Page'; import { filterProducts, productListFixture } from './fixture'
describe('product_list',()=>{it('renders fields and debug',()=>{const m=renderToStaticMarkup(<MemoryRouter initialEntries={['/products']}><Page/></MemoryRouter>); expect(m).toContain('商品列表页'); expect(m).toContain('车载吸尘器'); expect(m).toContain('继续处理'); expect(m).toContain('<details class="local-debug">')}); it('supports local filtering',()=>expect(filterProducts(productListFixture,'直发梳')).toHaveLength(1))})
