import { RoutePlaceholder } from '../RoutePlaceholder'
import { pageByKey } from '../pageMeta'

export default function TaskPackageExportPage() {
  return <RoutePlaceholder page={pageByKey.task_package_export} />
}
