import { RoutePlaceholder } from '../RoutePlaceholder'
import { pageByKey } from '../pageMeta'

export default function MaterialCheckPage() {
  return <RoutePlaceholder page={pageByKey.material_check} />
}
