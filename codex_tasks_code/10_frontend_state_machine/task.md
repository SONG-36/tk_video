# 任务 10：前端状态机与页面闸门

## 执行目标
实现第 8、9 章状态枚举、流转表、15 页进入条件、人工确认闸门和 `selected_variant_id` 切换失效标记；仅限前端本地纯状态层。第 17/18 章不新增主流程状态。
## 前置任务
- 任务 09 通过。未确认的状态兼容需求必须阻断。
## 必须读取的文件
- `00_design.md` 第 4、6、8、9、17、18、20、23、24 章。
- `docs/04_state_machine.md`、`docs/99_design_issues.md`、全部页面 README。
- 任务 09 路由、本任务 `task.md`/`check.md`。
## 允许修改的文件
- 前端 workflow/state 模块、路由守卫适配和专用单元测试；最小路由接入。
## 禁止修改的文件
- 设计/docs/rules/Schema/backend/data/outputs/scripts；页面表单、API、持久化。
## 具体实现范围
- 逐项实现第 8 章 21 个正式状态和第 9 章闸门。
- 为 15 页面定义进入条件；`product_list` 不推进状态。
- `target_variant_select` 后页面必须持有当前 `selected_variant_id` 上下文。
- 切换目标型号时，把未确认画像、报告、图片绑定、批次、镜头规划标为失效并要求复验；不得删除明确系列共享材料。
- 测试正常链、非法跳步、人工确认、blocked 和型号切换。
- 在既有页面状态内可增加四类未完成/风险标记：`TikTok 创意结构不完整`、`爆款模式未适配`、`产品适配未通过`、`剧情压过产品风险`；它们不是新状态，不改变页面顺序或自动推进流程。
## 禁止越界事项
- 不生成报告，不做类目、材料、图片、创意、批次或镜头判断；不接后端。
- 不新增自动确认、恢复、重试或历史兼容状态。
## 已知未裁决问题处理方式
- 第 20 章 `profile_completed` 与正式 `variant_profile_completed` 不一致：只使用第 8 章正式状态，不实现兼容映射。
- 其余六类问题只作为相关页面阻断原因，不进入状态枚举或业务判定。
## 验证命令或人工验收方式
- 运行前端 test/typecheck/build。
- 用表驱动测试覆盖 21 状态、15 页面、所有人工确认点和两次不同型号切换。
- `git diff --check`。
## 完成后输出
列出状态表、页面闸门、失效对象、测试矩阵和未实现的业务判断。
