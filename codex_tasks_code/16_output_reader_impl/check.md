# 任务 16 验收
## 文件变更检查
- [ ] 仅 reader、脚本接入和隔离模拟 outputs 测试变化。
## 功能检查
- [ ] 合法 outputs 可读；缺失/格式错可识别；结构化索引只在内存返回。
## 业务边界检查
- [ ] 不生成内容、不评价质量、不写索引、不调用 AI/API。
## ID 绑定检查
- [ ] 系列、型号、批次一致；只读白名单 video，未规划 04/05 被拒绝。
## 状态机/流程检查
- [ ] 任务包未导出不读；读取不自动进入 completed。
## 未裁决问题检查
- [ ] B/C 只标 unresolved，旧示例不扩大白名单。
## 测试/命令检查
- [ ] 缺失/格式/路径/软链/零写入和 `git diff --check` 通过。
## 是否可以进入下一任务
隔离读取通过、真实 outputs 零访问且无质量裁决后进入任务 17。
