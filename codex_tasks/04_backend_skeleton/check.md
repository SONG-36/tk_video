# 任务 04 人工验收

## 人工验收清单

- [ ] 执行前完整读取了 `00_design.md`。
- [ ] 15 个模块 README 均说明职责、输入、输出、依赖、状态/绑定和禁止行为。
- [ ] 后端总 README 明确后端不是大模型服务。
- [ ] 所有模块描述可独立交给后续实现者，不需要猜测职责边界。

## 文件是否存在

- [ ] 3 个型号/类目模块、3 个分类/打法模块、3 个材料/图片/创意模块 README 存在。
- [ ] 批次、镜头、报告、任务包、输出、validators 六个模块 README 存在。
- [ ] `backend/README.md` 和 `backend/app/README.md` 存在。
- [ ] 模块目录中没有新增业务源码。

## 内容是否符合 00_design.md

- [ ] 模块输入输出与第 7、10—18 章一致。
- [ ] 状态前置与第 8、9 章一致。
- [ ] 所有型号级模块明确使用当前 `selected_variant_id`。
- [ ] 批次、镜头、任务包明确使用三类 ID。
- [ ] output_reader 明确不读取未规划的 `video_04/05`。

## 是否存在越权修改

- [ ] `git diff` 只包含允许的后端 README 和必要问题记录。
- [ ] 未修改规则、Schema、测试、前端、脚本、任务或 `00_design.md`。
- [ ] 未创建业务代码、数据库、API、登录或外部模型调用。

## 是否存在业务规则被改写

- [ ] README 只引用规则文件和章节，没有复制后再改变规则。
- [ ] 没有新增状态、算法、评分、门槛或输出类型。
- [ ] 没有把模块 README、docs、rules 或 Schema 当权威源。
- [ ] 冲突只记录，没有自行设计解决方案。

## 是否可以进入下一任务的判断标准

只有全部模块职责完整、边界无歧义、范围外零改动、没有源码实现且与总设计逐项一致时，才可以进入任务 05。
