# 任务 02 人工验收

## 人工验收清单

- [ ] 执行前已完整读取 `00_design.md`。
- [ ] 11 个 JSON 均为可解析的机器可读规则骨架。
- [ ] 每个文件都有 `rule_set`、准确的 `source_section` 和骨架状态。
- [ ] 明确枚举、评分和阈值没有被不必要地留空。

## 文件是否存在

- [ ] 平台类目、型号、型号差异三个基础规则文件存在。
- [ ] 商品分类、视频分型、类目打法三个判断规则文件存在。
- [ ] 材料、图片、创意、视频批次、镜头规划五个生产规则文件存在。
- [ ] `rules/README.md` 明确机器规则不是权威源。

## 内容是否符合 00_design.md

- [ ] 商品分类覆盖类目和 5/4/3、3/4/4、2/4/3 评分与第 10 章一致。
- [ ] 五类视频分型、0—3 基础分、画像修正和限制与第 11 章一致。
- [ ] 材料等级、图片阈值、创意优先级、批次策略和镜头字段分别与第 12—16 章一致。
- [ ] 型号规则保留 `selected_variant_id` 和单型号默认对象逻辑。
- [ ] 三类 ID 与数据归属限制未被弱化。

## 是否存在越权修改

- [ ] `git diff` 只包含允许的 `rules/` 文件及必要的设计问题记录。
- [ ] 未修改 docs 阅读正文、Schema、前后端、脚本、任务文件或 `00_design.md`。
- [ ] 未创建规则执行代码、数据库、API、登录或模型调用。

## 是否存在业务规则被改写

- [ ] 没有新增、删除或重新解释任何规则值。
- [ ] 没有把案例字段或个人判断提升为通用规则。
- [ ] 没有把 rules、docs 或 Schema 当成新的权威源。
- [ ] 发现重复或冲突时已停止并记录，未擅自合并。

## 是否可以进入下一任务的判断标准

只有全部 JSON 可解析、来源准确、关键规则与权威源逐项一致、允许范围外零改动时，才可以进入任务 03；存在来源不明、规则值漂移或冲突未记录时不得继续。
