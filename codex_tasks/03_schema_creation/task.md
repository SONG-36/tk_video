# 任务：JSON Schema 建立

执行前必须先完整读取根目录 `00_design.md`，并以其为唯一权威源。

根据第 18 章补全全部 Schema。明确对象、必填字段和绑定关系，重点校验 `selected_variant_id` 对应 `variant_id`，以及批次、镜头、任务包的 `product_series_id + variant_id + batch_id` 一致性。
