# 任务 07 人工验收

## 人工验收清单

- [ ] 执行前完整读取了 `00_design.md`。
- [ ] 输出读取顺序、路径边界、视频白名单和缺失处理写清楚。
- [ ] 后端模块与前端页面只有职责和数据契约，没有实现代码。
- [ ] 脚本运行无副作用，不遍历、不创建、不修改真实输出。

## 文件是否存在

- [ ] `docs/07_output_spec.md` 和相关质量说明存在。
- [ ] `backend/app/output_reader/README.md` 存在。
- [ ] `frontend/pages/output_viewer/README.md` 存在。
- [ ] `scripts/read_outputs.py` 占位存在。
- [ ] `.vscode/tasks.json` 有且只有一个 `read-outputs` 入口。
- [ ] `outputs/README.md` 和 `.gitkeep` 存在，目录内无真实内容资产。

## 内容是否符合 00_design.md

- [ ] 根路径严格为 `outputs/{product_series_id}/{batch_id}/`。
- [ ] 读取前使用已确认批次计划确定允许的 `video_id`。
- [ ] 没有规划时不读取 `video_04/05`，即使目录意外存在。
- [ ] 展示字段覆盖第 17、19 章规定的画板、脚本、故事板、镜头、提示词、输入、剪辑、质检、缺失和风险。
- [ ] 路径不能越过指定商品系列和批次根。
- [ ] 缺失文件只报告，不伪造、不自动修复。

## 是否存在越权修改

- [ ] `git diff` 只包含允许的说明、README、占位脚本和单个 VSCode 入口。
- [ ] 未修改 rules、Schema、其他模块/页面/脚本、任务或 `00_design.md`。
- [ ] 未创建 UI、API、数据库、登录、监控服务或模型调用。
- [ ] 未创建、删除、重命名或写入真实 outputs 文件。

## 是否存在业务规则被改写

- [ ] 没有新增文件类型、显示字段、读取状态或目录策略。
- [ ] 没有把目录存在误当作批次规划存在。
- [ ] 没有把输出说明、docs、rules 或 Schema 当成权威源。
- [ ] 冲突已记录，没有自行采用旧输出结构。

## 是否可以进入下一任务的判断标准

只有读取契约完整、路径和批次限制准确、脚本无副作用、范围外零改动且所有质量字段可回溯到总设计时，任务 07 才算通过；否则不得进入真实输出读取实现。
