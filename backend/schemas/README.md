# schemas 目录职责

本目录负责保存 `00_design.md` 第 18 章业务对象的 JSON Schema 骨架，说明对象、必填字段和绑定关系。

本目录不负责创造字段、改变数据含义或替代 `00_design.md`；Schema 不是新的权威源。

未来只能依据已确认的权威源变更补全字段和校验约束，并保持 `product_series_id + variant_id + batch_id` 的绑定关系。
