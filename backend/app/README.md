# app 模块职责

本目录负责按 `00_design.md` 中的判断器、检查器、规划器、生成器、读取器和校验器拆分后端职责。模块只通过明确输入输出、`rules/` 和 Schema 协作。

本目录不负责自由生成业务规则，不是大模型服务，也不直接调用外部视频模型。

未来实现只能在现有模块边界内扩展，并必须服从根目录 `00_design.md` 的流程闸门、型号隔离和三类 ID 绑定。
