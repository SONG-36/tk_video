# backend 目录职责

本目录负责规则判断、差异检查、报告与计划生成、任务包文件生成、输出读取和流程校验。模块对应 `00_design.md` 已定义的判断器、检查器、生成器或读取器。

本目录不是大模型服务，不负责自由生成业务规则，不直接调用 Seedance / 即梦，也不引入数据库或登录权限。

未来实现应让模块读取 `rules/`，使用 `schemas/` 校验输入输出，并通过 `tests/` 验证状态闸门和三类 ID 的绑定关系。
